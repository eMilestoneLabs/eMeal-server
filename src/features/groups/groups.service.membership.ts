/**
 * Membership management methods — split into separate file for readability.
 * These are mixed into GroupsService via service injection (not extension).
 *
 * All methods defined directly on GroupsService in groups.service.ts.
 * This file provides the implementation stubs for IDE navigation — actual
 * methods are in groups.service.ts.
 */
export {}; // This file is a documentation stub only — see groups.service.ts
