node_modules/
dist/
.env
.env.local
.env.*.local
.env.production
# timestamped env backups created by rotate-secrets.sh / manual edits — never commit,
# and keep them from tripping deploy.sh's clean-tree gate
.env*.bak.*
.env.bak.*
prisma/migrations/*.sql
logs/
*.log
coverage/

# Master-audit run outputs (deploy/run.sh) — reports stay on the box, never
# committed, and never trip deploy.sh's clean-tree gate
deploy/audit-reports/
deploy/CERTIFICATION_*.md
.DS_Store
.vscode/

# Temporary backup files
*_backup.json
bb_deps_backup.json
devdeps_backup.json

# Dev-only docs and guides (not part of prod source)
docs/FLUTTER_UI_SCREENSHOT
docs/PERMANENT_MEMORY_CORE_DEVELOPMENT
BACKEND_IMPLEMENTATION_EXECUTION_GUIDE.md
BACKEND_IMPLEMENTATION_PLAN.md
FINAL_MASTER_BACKEND_GUIDE.md
FINAL-DEEP-AUDIT-REPORT-B1-B7.md
SETUP.md
B2_VERIFY.sh
B3_VERIFY.sh
B4_VERIFY.sh
B5_VERIFY.sh
B6_VERIFY.sh
B7_VERIFY.sh
CLAUDE.md
docs/NOTIFICATION_OTP_PLAN.md
docs/PERGROUP_ROLE_AND_ZEROMEAL_ROLLOUT_2026-06-15.md
docs/SOURCE_OF_REQUIREMENTS_END_USER_FEATURES/DETAILED_WORKFLOWS/

# CI-generated (export artifacts, coverage reports)
artifacts/

# Performance heap snapshots (perf-test.js writes these transiently before
# moving them into artifacts/; never commit raw root-level snapshots)
*.heapsnapshot
